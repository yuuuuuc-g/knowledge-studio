{
  "name": "knowledge-studio",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "A frontend/backend separated knowledge reading, whiteboard, writing, and AI studio.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "check": "node --check apps/api/src/server.js && npm run build -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@tiptap/extension-placeholder": "^3.26.0",
    "@tiptap/react": "^3.26.0",
    "@tiptap/starter-kit": "^3.26.0",
    "multer": "^2.1.1"
  }
}
